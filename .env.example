# Copy to .env at the project root. Never commit the filled-in file.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Runtime: restricted habit_api user, transaction pooler port 6543.
DATABASE_URL=
# Migration-only: admin direct connection or session pooler (5432). Never add to Vercel.
MIGRATION_DATABASE_URL=
