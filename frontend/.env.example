# Copy to frontend/.env.local. These two values are deliberately public.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_REPLACE_ME
# Never put database credentials, secret keys or service-role keys in VITE_ variables.
